package domain

// EmbeddingProviderHash names the one built-in provider: hashembed.Embedder,
// the dependency-free feature-hashing pseudo-embedding. Every other choice
// is an EmbeddingHTTPEndpoint.ID.
const EmbeddingProviderHash = "hash"
