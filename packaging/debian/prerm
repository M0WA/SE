#!/bin/sh
set -e
if command -v systemctl >/dev/null 2>&1 && [ -d /run/systemd/system ]; then
    if systemctl is-active --quiet searchengine; then
        systemctl stop searchengine
    fi
    systemctl disable searchengine.service || true
fi
exit 0
